# Workspace preferences

- After testing, stop the frontend and backend servers and verify their ports are closed (normally 3000 and 8000). Also stop any isolated test servers started for the task. Do not leave or restart servers at the end unless the user explicitly requests it.
- Preserve project files and saved projects. Remove temporary testing artifacts when verification is finished.
