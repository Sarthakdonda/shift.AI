NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# On a phone the app rewrites a localhost API base to the computer that served
# this page, so the same value works over Wi-Fi without further configuration.
# Hosted app deployment: NEXT_PUBLIC_API_BASE_URL=https://your-backend.example
